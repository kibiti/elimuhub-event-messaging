import csv

def read_contacts_csv(file_path):
    contacts = []
    with open(file_path, newline='', encoding='utf-8') as csvfile:
        reader = csv.DictReader(csvfile)
        for row in reader:
            contact = {
                "first_name": row.get("First Name", "").strip(),
                "middle_name": row.get("Middle Name", "").strip(),
                "last_name": row.get("Last Name", "").strip(),
                "organization": row.get("Organization Name", row.get("Company", "")).strip(),
                "email": row.get("E-mail Address", row.get("E-mail 1 - Value", "")).strip(),
                "phone": row.get("Primary Phone", row.get("Phone 1 - Value", "")).strip(),
                "labels": row.get("Categories", row.get("Labels", "")).strip(),
                "notes": row.get("Notes", "").strip(),
            }
            contacts.append(contact)
    return contacts

def print_contacts(contacts):
    for contact in contacts:
        print("Name:", contact["first_name"], contact["middle_name"], contact["last_name"])
        print("Organization:", contact["organization"])
        print("Email:", contact["email"])
        print("Phone:", contact["phone"])
        print("Labels:", contact["labels"])
        print("Notes:", contact["notes"])
        print("-" * 40)

if __name__ == "__main__":
    file_path = "contacts_examples/sample_contacts.csv"
    contacts = read_contacts_csv(file_path)
    print_contacts(contacts)
